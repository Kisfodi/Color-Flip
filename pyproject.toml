[project]
name = "color-flip-python"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "numpy>=2.4.2",
    "pyyaml>=6.0",
    "pytest>=9.0.2",
    "gymnasium>=1.2.3",
    #'pygame>=2.6.1',
    "fastapi>=0.139.2",
    "pydantic>=2.13.4",
]

# [build-system]
# requires = ["flit_core<4"]
# build-backend = "flit_core.buildapi"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
branch = true
